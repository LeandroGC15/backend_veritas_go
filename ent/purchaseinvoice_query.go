// Code generated by ent, DO NOT EDIT.

package ent

import (
	"Veritasbackend/ent/predicate"
	"Veritasbackend/ent/purchaseinvoice"
	"Veritasbackend/ent/supplier"
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// PurchaseInvoiceQuery is the builder for querying PurchaseInvoice entities.
type PurchaseInvoiceQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.PurchaseInvoice
	// eager-loading edges.
	withSupplier *SupplierQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PurchaseInvoiceQuery builder.
func (piq *PurchaseInvoiceQuery) Where(ps ...predicate.PurchaseInvoice) *PurchaseInvoiceQuery {
	piq.predicates = append(piq.predicates, ps...)
	return piq
}

// Limit adds a limit step to the query.
func (piq *PurchaseInvoiceQuery) Limit(limit int) *PurchaseInvoiceQuery {
	piq.limit = &limit
	return piq
}

// Offset adds an offset step to the query.
func (piq *PurchaseInvoiceQuery) Offset(offset int) *PurchaseInvoiceQuery {
	piq.offset = &offset
	return piq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (piq *PurchaseInvoiceQuery) Unique(unique bool) *PurchaseInvoiceQuery {
	piq.unique = &unique
	return piq
}

// Order adds an order step to the query.
func (piq *PurchaseInvoiceQuery) Order(o ...OrderFunc) *PurchaseInvoiceQuery {
	piq.order = append(piq.order, o...)
	return piq
}

// QuerySupplier chains the current query on the "supplier" edge.
func (piq *PurchaseInvoiceQuery) QuerySupplier() *SupplierQuery {
	query := &SupplierQuery{config: piq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := piq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := piq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(purchaseinvoice.Table, purchaseinvoice.FieldID, selector),
			sqlgraph.To(supplier.Table, supplier.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, purchaseinvoice.SupplierTable, purchaseinvoice.SupplierColumn),
		)
		fromU = sqlgraph.SetNeighbors(piq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first PurchaseInvoice entity from the query.
// Returns a *NotFoundError when no PurchaseInvoice was found.
func (piq *PurchaseInvoiceQuery) First(ctx context.Context) (*PurchaseInvoice, error) {
	nodes, err := piq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{purchaseinvoice.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (piq *PurchaseInvoiceQuery) FirstX(ctx context.Context) *PurchaseInvoice {
	node, err := piq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first PurchaseInvoice ID from the query.
// Returns a *NotFoundError when no PurchaseInvoice ID was found.
func (piq *PurchaseInvoiceQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = piq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{purchaseinvoice.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (piq *PurchaseInvoiceQuery) FirstIDX(ctx context.Context) int {
	id, err := piq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single PurchaseInvoice entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one PurchaseInvoice entity is found.
// Returns a *NotFoundError when no PurchaseInvoice entities are found.
func (piq *PurchaseInvoiceQuery) Only(ctx context.Context) (*PurchaseInvoice, error) {
	nodes, err := piq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{purchaseinvoice.Label}
	default:
		return nil, &NotSingularError{purchaseinvoice.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (piq *PurchaseInvoiceQuery) OnlyX(ctx context.Context) *PurchaseInvoice {
	node, err := piq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only PurchaseInvoice ID in the query.
// Returns a *NotSingularError when more than one PurchaseInvoice ID is found.
// Returns a *NotFoundError when no entities are found.
func (piq *PurchaseInvoiceQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = piq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{purchaseinvoice.Label}
	default:
		err = &NotSingularError{purchaseinvoice.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (piq *PurchaseInvoiceQuery) OnlyIDX(ctx context.Context) int {
	id, err := piq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of PurchaseInvoices.
func (piq *PurchaseInvoiceQuery) All(ctx context.Context) ([]*PurchaseInvoice, error) {
	if err := piq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return piq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (piq *PurchaseInvoiceQuery) AllX(ctx context.Context) []*PurchaseInvoice {
	nodes, err := piq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of PurchaseInvoice IDs.
func (piq *PurchaseInvoiceQuery) IDs(ctx context.Context) ([]int, error) {
	var ids []int
	if err := piq.Select(purchaseinvoice.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (piq *PurchaseInvoiceQuery) IDsX(ctx context.Context) []int {
	ids, err := piq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (piq *PurchaseInvoiceQuery) Count(ctx context.Context) (int, error) {
	if err := piq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return piq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (piq *PurchaseInvoiceQuery) CountX(ctx context.Context) int {
	count, err := piq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (piq *PurchaseInvoiceQuery) Exist(ctx context.Context) (bool, error) {
	if err := piq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return piq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (piq *PurchaseInvoiceQuery) ExistX(ctx context.Context) bool {
	exist, err := piq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PurchaseInvoiceQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (piq *PurchaseInvoiceQuery) Clone() *PurchaseInvoiceQuery {
	if piq == nil {
		return nil
	}
	return &PurchaseInvoiceQuery{
		config:       piq.config,
		limit:        piq.limit,
		offset:       piq.offset,
		order:        append([]OrderFunc{}, piq.order...),
		predicates:   append([]predicate.PurchaseInvoice{}, piq.predicates...),
		withSupplier: piq.withSupplier.Clone(),
		// clone intermediate query.
		sql:    piq.sql.Clone(),
		path:   piq.path,
		unique: piq.unique,
	}
}

// WithSupplier tells the query-builder to eager-load the nodes that are connected to
// the "supplier" edge. The optional arguments are used to configure the query builder of the edge.
func (piq *PurchaseInvoiceQuery) WithSupplier(opts ...func(*SupplierQuery)) *PurchaseInvoiceQuery {
	query := &SupplierQuery{config: piq.config}
	for _, opt := range opts {
		opt(query)
	}
	piq.withSupplier = query
	return piq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		InvoiceNumber string `json:"invoice_number,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.PurchaseInvoice.Query().
//		GroupBy(purchaseinvoice.FieldInvoiceNumber).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
//
func (piq *PurchaseInvoiceQuery) GroupBy(field string, fields ...string) *PurchaseInvoiceGroupBy {
	grbuild := &PurchaseInvoiceGroupBy{config: piq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := piq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return piq.sqlQuery(ctx), nil
	}
	grbuild.label = purchaseinvoice.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		InvoiceNumber string `json:"invoice_number,omitempty"`
//	}
//
//	client.PurchaseInvoice.Query().
//		Select(purchaseinvoice.FieldInvoiceNumber).
//		Scan(ctx, &v)
//
func (piq *PurchaseInvoiceQuery) Select(fields ...string) *PurchaseInvoiceSelect {
	piq.fields = append(piq.fields, fields...)
	selbuild := &PurchaseInvoiceSelect{PurchaseInvoiceQuery: piq}
	selbuild.label = purchaseinvoice.Label
	selbuild.flds, selbuild.scan = &piq.fields, selbuild.Scan
	return selbuild
}

func (piq *PurchaseInvoiceQuery) prepareQuery(ctx context.Context) error {
	for _, f := range piq.fields {
		if !purchaseinvoice.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if piq.path != nil {
		prev, err := piq.path(ctx)
		if err != nil {
			return err
		}
		piq.sql = prev
	}
	return nil
}

func (piq *PurchaseInvoiceQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*PurchaseInvoice, error) {
	var (
		nodes       = []*PurchaseInvoice{}
		_spec       = piq.querySpec()
		loadedTypes = [1]bool{
			piq.withSupplier != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*PurchaseInvoice).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &PurchaseInvoice{config: piq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, piq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}

	if query := piq.withSupplier; query != nil {
		ids := make([]int, 0, len(nodes))
		nodeids := make(map[int][]*PurchaseInvoice)
		for i := range nodes {
			fk := nodes[i].SupplierID
			if _, ok := nodeids[fk]; !ok {
				ids = append(ids, fk)
			}
			nodeids[fk] = append(nodeids[fk], nodes[i])
		}
		query.Where(supplier.IDIn(ids...))
		neighbors, err := query.All(ctx)
		if err != nil {
			return nil, err
		}
		for _, n := range neighbors {
			nodes, ok := nodeids[n.ID]
			if !ok {
				return nil, fmt.Errorf(`unexpected foreign-key "supplier_id" returned %v`, n.ID)
			}
			for i := range nodes {
				nodes[i].Edges.Supplier = n
			}
		}
	}

	return nodes, nil
}

func (piq *PurchaseInvoiceQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := piq.querySpec()
	_spec.Node.Columns = piq.fields
	if len(piq.fields) > 0 {
		_spec.Unique = piq.unique != nil && *piq.unique
	}
	return sqlgraph.CountNodes(ctx, piq.driver, _spec)
}

func (piq *PurchaseInvoiceQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := piq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (piq *PurchaseInvoiceQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   purchaseinvoice.Table,
			Columns: purchaseinvoice.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: purchaseinvoice.FieldID,
			},
		},
		From:   piq.sql,
		Unique: true,
	}
	if unique := piq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := piq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, purchaseinvoice.FieldID)
		for i := range fields {
			if fields[i] != purchaseinvoice.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := piq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := piq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := piq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := piq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (piq *PurchaseInvoiceQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(piq.driver.Dialect())
	t1 := builder.Table(purchaseinvoice.Table)
	columns := piq.fields
	if len(columns) == 0 {
		columns = purchaseinvoice.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if piq.sql != nil {
		selector = piq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if piq.unique != nil && *piq.unique {
		selector.Distinct()
	}
	for _, p := range piq.predicates {
		p(selector)
	}
	for _, p := range piq.order {
		p(selector)
	}
	if offset := piq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := piq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// PurchaseInvoiceGroupBy is the group-by builder for PurchaseInvoice entities.
type PurchaseInvoiceGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (pigb *PurchaseInvoiceGroupBy) Aggregate(fns ...AggregateFunc) *PurchaseInvoiceGroupBy {
	pigb.fns = append(pigb.fns, fns...)
	return pigb
}

// Scan applies the group-by query and scans the result into the given value.
func (pigb *PurchaseInvoiceGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := pigb.path(ctx)
	if err != nil {
		return err
	}
	pigb.sql = query
	return pigb.sqlScan(ctx, v)
}

func (pigb *PurchaseInvoiceGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range pigb.fields {
		if !purchaseinvoice.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := pigb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pigb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (pigb *PurchaseInvoiceGroupBy) sqlQuery() *sql.Selector {
	selector := pigb.sql.Select()
	aggregation := make([]string, 0, len(pigb.fns))
	for _, fn := range pigb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(pigb.fields)+len(pigb.fns))
		for _, f := range pigb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(pigb.fields...)...)
}

// PurchaseInvoiceSelect is the builder for selecting fields of PurchaseInvoice entities.
type PurchaseInvoiceSelect struct {
	*PurchaseInvoiceQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (pis *PurchaseInvoiceSelect) Scan(ctx context.Context, v interface{}) error {
	if err := pis.prepareQuery(ctx); err != nil {
		return err
	}
	pis.sql = pis.PurchaseInvoiceQuery.sqlQuery(ctx)
	return pis.sqlScan(ctx, v)
}

func (pis *PurchaseInvoiceSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := pis.sql.Query()
	if err := pis.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
